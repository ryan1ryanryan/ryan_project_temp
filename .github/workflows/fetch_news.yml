name: AI News Update
on:
  schedule:
    - cron: '0 * * * *' # Runs every hour
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          pip install requests google-genai

      - name: Fetch Raw News
        run: |
          curl "https://newsapi.org/v2/everything?q=tech+OR+gaming&language=en&pageSize=100&apiKey=${{ secrets.NEWS_API_KEY }}" -o raw_news.json

      - name: Run AI Processor
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          USER_LIKES: "gaming, pc hardware, tech"
          USER_IGNORED: "politics, news"
        run: python process_news.py

      - name: Commit and Push
        run: |
          git config --global user.name "Ryan-AI-Bot"
          git config --global user.email "bot@github.com"
          git add news.json
          git commit -m "Automated Semantic Rank Update" || echo "No changes"
          git push origin main
